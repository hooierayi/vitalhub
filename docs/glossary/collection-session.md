# 采集会话

## 1. 定义

- 定义：一次用户采集任务的关联标识。
- 作用范围：关联前后问卷、设备、采集片段、上传与诊断结果。
- 首页展示四步进度：采集前问卷、连接设备、开始采集、采集后问卷；AI 分析不计入该进度。
- 常见代码关键词：`sessionId`、`RouteArgs.SESSION_ID`。

## 2. 相关位置

| 类型 | 路径/模块 | 说明 |
|---|---|---|
| 路由参数 | `:core:common` | `RouteArgs.SESSION_ID` 规定跨模块参数名 |
| 业务链路 | feature 模块 | 问卷、设备、采集和分析跳转均接收该参数 |
| 架构证据 | `ARCHITECTURE.md` | 明确列出会话关联范围 |

## 3. 相关功能文档

| 功能 | 文档 | 使用场景 |
|---|---|---|
| 采集会话链路 | 待建立 | 记录端到端状态流转时创建 |

## 5. 首页流程检查点

- 前问卷提交、设备连接成功、采集正常结束、后问卷提交分别对应 1/4 至 4/4。
- 设备连接或采集中断时，因 BLE 连接不可恢复，流程回退到 1/4 并要求重新连接设备。
- 后问卷提交后返回首页保留 4/4；AI 分析不属于首页流程。

## 4. 易混淆概念

| 概念 | 区别 |
|---|---|
| 连续记录 | `recordId` 指向记录仪创建的连续记录文件，不等同于整个采集任务。 |

## 5. 校验记录

- 术语来源：`ARCHITECTURE.md`。
- 注释/文档证据：核心标识章节与 `RouteArgs.SESSION_ID`。
- 最近校验：2026-08-19。
