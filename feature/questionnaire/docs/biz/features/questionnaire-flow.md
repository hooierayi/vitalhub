# 问卷流程

## 页面行为

- 采集前、采集后问卷共用 `QuestionnaireFragment`，按 `questionnairePhase` 选择题目。
- 每次打开问卷均从第一页和空答案开始，不自动回填当前会话或历史会话的答案。
- 当前会话和问卷阶段存在最近保存的答案时，标题栏最右侧展示“导入上次记录”；没有记录时不展示该按钮。
- 导入后答案立即写入当前会话草稿，用户仍需检查并完成问卷提交。
- 用户作答、翻页或提交时都会更新当前会话可导入的最近记录。
